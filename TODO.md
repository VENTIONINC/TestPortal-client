- [x] **Set up a new Vite React project:** Use `npm create vite@latest your-project-name -- --template react` (or `yarn create vite your-project-name --template react` or `pnpm create vite your-project-name --template react`) to scaffold a new React project with Vite.
- [x] **Analyze Svelte Project Structure:**
  - [x] Identify all Svelte components (usually `.svelte` files in `src/lib` or `src/components`).
  - [x] Identify Svelte stores (often in a `src/stores` directory or similar).
  - [x] Identify routing setup (e.g., `svelte-routing`, `svelte-spa-router`, or a custom solution).
  - [x] Identify utility functions/modules (e.g., in `src/utils` or `src/lib`).
  - [x] Identify global styles and how they are applied.
  - [x] List all static assets (images, fonts, etc.) and their locations.
  - [x] Review `package.json` for Svelte-specific dependencies and any other relevant packages.
- [x] **Install Core React Dependencies:**
  - [x] `react`, `react-dom` (should be included by Vite template).
  - [-] `react-router-dom` for routing (if needed).
  - [-] State management library if desired (e.g., Redux Toolkit, Zustand, Jotai, or use React Context).
- [x] **Set up Project Structure in Vite/React Project:**
  - [x] Create directories like `src/components`, `src/pages` (or `src/views`), `src/hooks`, `src/contexts`, `src/services`, `src/utils`, `src/assets`, `src/styles`.
- [x] **Migrate Static Assets:**
  - [x] Copy static assets (images, fonts, etc.) from your Svelte project's static/public folder to the `public` folder in your Vite project.
  - [ ] Update paths in your new React components if necessary.
- [x] **Migrate Global Styles:**
  - [x] Copy global CSS files (e.g., `app.css`, `global.css`) into the `src` directory (e.g. `src/index.css` or `src/styles/global.css`) and import it in `src/main.jsx` (or `src/main.tsx`).
  - [x] If using CSS preprocessors (Sass, Less), install the necessary Vite preprocessor support (e.g., `npm install -D sass`). (Marked as N/A or complete as none were detected)
- [x] **Migrate Utility Functions:**
  - [x] Copy JavaScript/TypeScript utility functions to `src/utils` (or similar).
  - [x] Review and update them if they have Svelte-specific logic or dependencies.
- [-] **Set up Routing (if applicable):** (Skipped, Svelte app uses tab-based navigation)
  - [-] Install `react-router-dom`: `npm install react-router-dom`.
  - [-] Define routes in your `App.jsx` or a dedicated routing configuration file (e.g., `src/router.jsx`).
  - [-] Recreate page-level components (previously Svelte routes/pages) as React components in `src/pages`.
- [x] **Migrate Svelte Stores to React State Management:**
  - [x] **For simple stores:** Consider React's `useState`, `useReducer`, or `Context API`. (Used `useState` in `useFilterParams` hook)
  - [x] **For complex state:** Evaluate and implement a chosen state management library (Redux Toolkit, Zustand, etc.). (Used `TanStack Query` for server state via `useResultsQuery` hook)
  - [x] Re-implement the logic of your Svelte stores using the chosen React mechanism. (Implemented `useFilterParams` and `useResultsQuery` hooks)
- [ ] **Migrate Svelte Components to React Components:**
  - [ ] For each Svelte component:
    - [x] Create a corresponding React component file (e.g., `.jsx` or `.tsx`) in `src/components`. (For App.tsx, done in src/)
    - [x] Translate the Svelte template (HTML structure) to JSX. (For App.tsx)
      - [x] Svelte directives (`#if`, `#each`, `#await`) to JSX conditional rendering, `.map()`, etc. (For App.tsx)
      - [x] Svelte event handlers (`on:click`) to React event handlers (`onClick`). (For App.tsx)
      - [x] Svelte bindings (`bind:value`) to React controlled components or `useRef`. (N/A for App.tsx)
    - [x] Translate the Svelte script logic (JavaScript/TypeScript) to React component logic. (For App.tsx)
      - [x] Props: Svelte `export let propName` becomes React function component parameters `(props)` or destructuring `({ propName })`. (N/A for App.tsx, handled state)
      - [x] Reactive statements (`$: ...`): Use `useEffect` or derive state within the render function. (N/A for App.tsx, handled state with hooks)
      - [x] Lifecycle functions (`onMount`, `onDestroy`): Use `useEffect` with appropriate dependency arrays. (N/A for App.tsx, data fetching in hook)
    - [x] Translate Svelte component styles: (For App.css from App.svelte)
      - [-] Option 1: CSS Modules (create `ComponentName.module.css` and import).
      - [-] Option 2: Styled-components or Emotion.
      - [x] Option 3: Global or component-level CSS files imported directly. (Used App.css for App.tsx)
  - [ ] (Next: components/results.svelte)
  - [ ] (Next: components/issues.svelte)
- [ ] **Update `package.json`:**
  - [ ] Remove Svelte-specific dependencies (e.g., `svelte`, `svelte-loader`, Svelte routing packages, Svelte preprocessors).
  - [ ] Add any new React-specific dependencies that weren't added by the Vite template or during migration steps.
  - [ ] Review and update scripts in `package.json` (e.g., `dev`, `build`, `preview`).
- [ ] **Testing:**
  - [ ] Set up a React testing environment (e.g., Vitest, Jest with React Testing Library).
  - [ ] Write new unit and integration tests for your React components and hooks.
  - [ ] Migrate existing end-to-end tests if applicable, updating selectors and interactions.
- [ ] **Linting and Formatting:**
  - [ ] Configure ESLint and Prettier for React/JSX/TypeScript.
  - [ ] Ensure Vite's ESLint plugin is set up correctly if you're using it.
- [ ] **Address API Calls / Data Fetching:**
  - [ ] Update how data is fetched. Common React patterns include using `useEffect` with `fetch` or a library like Axios, or using data fetching libraries like React Query (TanStack Query) or SWR.
- [ ] **Final Review and Cleanup:**
  - [ ] Remove any leftover Svelte files or configurations.
  - [ ] Check for console errors and warnings.
  - [ ] Perform thorough manual testing of the application.
  - [ ] Optimize performance if needed (e.g., using `React.memo`, `useCallback`, code splitting).
- [ ] **Build and Deploy:**
  - [ ] Run `npm run build` (or `yarn build` / `pnpm build`).
  - [ ] Configure your deployment environment for a Vite/React static build (typically from the `dist` folder).
